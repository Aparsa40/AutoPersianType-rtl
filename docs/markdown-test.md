# Full Markdown Rendering Test

این فایل صرفاً برای تست ادیتور نوشته شده است.
هدف این است که ببینیم آیا:

هدینگ‌ها درست رندر می‌شوند؟

متن RTL و LTR قاطی به‌هم نمی‌ریزد؟

لیست‌ها، جدول‌ها، کد بلاک‌ها و نقل‌قول‌ها درست نمایش داده می‌شوند؟

🔹 مقدمه (Introduction)

مارک‌داون یک زبان سبک‌وزن (Lightweight Markup Language) است که برای نوشتن متن‌های ساخت‌یافته استفاده می‌شود.

Markdown is widely used in:

Documentation

README files

Note-taking apps

Static site generators

نکته: اگر این متن را می‌خوانی و همه‌چیز مرتب است، یعنی ادیتورت کارت را درست انجام می‌دهد.

## 🧱 هدینگ‌ها (Headings)

### Heading Level 3

#### Heading Level 4

##### Heading Level 5

###### Heading Level 6

متن ساده زیر هدینگ‌ها.

✍️ استایل متن (Text Styling)

این متن **Bold** است

این متن *Italic* است

این متن ***Bold + Italic*** است

~~این متن خط‌خورده است~~

این متن `inline code` دارد

ترکیبی فارسی و English داخل یک جمله بدون به‌هم‌ریختگی.

🔗 لینک‌ها (Links)

لینک ساده: [Google](https://google.com)

لینک با توضیح فارسی: [مستندات Markdown](https://commonmark.org)

📦 لیست‌ها (Lists)

- لیست بدون ترتیب (Unordered)
  - آیتم اول
    - زیرآیتم اول
    - زیرآیتم دوم
  - آیتم دوم
  - آیتم سوم

1. لیست ترتیبی (Ordered)
2. مرحله دوم
3. مرحله سوم
   1. زیرمرحله ۳.۱
   2. زیرمرحله ۳.۲

☑️ چک‌لیست (Task List)

- [ ] رندر هدینگ
- [ ] رندر متن فارسی
- [ ] رندر متن انگلیسی
- [ ] بررسی نهایی بیلد
- [ ] تست در حالت Production

💬 نقل‌قول (Blockquote)

> این یک نقل‌قول ساده است
>>
> This is a multiline blockquote
> that should render correctly.

🧠 نقل‌قول تو در تو

> سطح اول
>> سطح دوم
>>> سطح سوم

💻 کد بلاک (Code Blocks)

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("Amir");
```

```typescript
interface User {
  id: number;
  name: string;
  active: boolean;
}

const user: User = {
  id: 1,
  name: "Amir Hossein",
  active: true,
};
```

```bash
npm install
npm run dev
npm run build
```

🧩 جدول (Table)

| Feature | Supported | Notes |
| --- | --- | --- |
| RTL Text | ✅ | فارسی درست نمایش داده می‌شود |
| LTR Text | ✅ | English OK |
| Code Block | ✅ | Syntax Highlight |
| Table | ✅ | Alignment OK |

🖼 تصویر (Image – تست رندر)

![تست تصویر](https://via.placeholder.com/600x120)

➖ جداکننده‌ها (Horizontal Rules)

---

🧪 ترکیب RTL و LTR در یک خط

این یک تست است for mixing Persian متن with English words inside the same sentence.

📌 نکات پایانی (Final Notes)

اگر فاصله‌ها درست است → عالی

اگر بولد و ایتالیک قاطی نشده → عالی

اگر جدول نریخته → عالی

اگر RTL/LTR قاطی نشده → ادیتورت حرفه‌ایه 😄

✅ پایان فایل

End of Markdown Test File
